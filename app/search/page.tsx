import Link from "next/link";import {Search as SearchIcon} from "lucide-react";import {articles} from "@/lib/articles";import {Footer,Masthead,StoryMeta} from "@/components/editorial";export const metadata={title:"Search"};export default function Search(){return <main><Masthead/><section className="search-page"><span>O.R / Search the archive</span><form><label htmlFor="q">Search stories, records and authors</label><div><input id="q" placeholder="Try “water”, “systems” or “Record 0421”"/><button aria-label="Search"><SearchIcon/></button></div></form><h1>From the archive</h1>{articles.slice(0,5).map(a=><article key={a.slug}><StoryMeta article={a}/><h2><Link href={`/article/${a.slug}`}>{a.title}</Link></h2></article>)}</section><Footer/></main>}
