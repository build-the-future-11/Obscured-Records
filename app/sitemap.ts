import type {MetadataRoute} from "next";import {articles,sections} from "@/lib/articles";const base="https://obscured-records.ryangomez-hs.chatgpt.site";export default function sitemap():MetadataRoute.Sitemap{const routes=["","latest",...sections.map(s=>s.toLowerCase()),"people","opinion","about","submit","newsletter","search"];return [...routes.map(r=>({url:`${base}/${r}`,lastModified:new Date("2026-09-14")})),...articles.map(a=>({url:`${base}/article/${a.slug}`,lastModified:new Date("2026-09-14")}))]}
